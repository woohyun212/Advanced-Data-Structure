# 이슈/PR 별 버그 및 해결 내역 정리
## Issue 기반 정리

Issue #26 – Play_1() 무한 재귀
- 문제 증상  
    - Play_1() 함수가 자기 자신을 계속 재귀 호출하여 무한 루프 / 스택 오버플로우가 발생함.
- 해결 내역  
    - PR #38 [Fix] issue#26 무한 재귀 해결에서 재귀 구조를 수정하여 무한 재귀가 더 이상 발생하지 않도록 처리하고 병합됨.

---

Issue #27 – Battle() 텍스트 출력 최적화 필요
- 문제 증상  
    - Battle() 함수 내에서 텍스트 출력 시 지연(delay) 처리 및 범위 검증(l 변수 사용)이 비효율적이거나 잘못되어 출력/성능 문제가 발생함.
- 해결 내역  
    - PR #64 [Fix] #27 #28 issue 해결; Battle() 함수 수정에서 출력 로직 및 관련 검증을 수정해 최적화하고 병합됨.
추가적으로 PR #60 issue#27 battle 함수 최적화에서 Battle 함수 성능/로직을 최적화함.

---

Issue #28 – Battle() 리소스 누수
- 문제 증상  
    - 퀘스트 데이터 파일을 fopen으로 연 뒤 fclose를 하지 않거나, fopen 실패 시 NULL 체크를 하지 않아 파일 핸들/리소스 누수가 발생함.
- 해결 내역  
    - PR #64에서 파일 열기/닫기 및 예외 처리 로직을 수정하여 리소스 누수를 해소하고 병합됨.

---

Issue #29 – Potion() 물약 능력 분리 오류
- 문제 증상  
    - 물약 사용 시 최대 체력 / 현재 체력 처리 로직(user.nhp 등)을 잘못 사용하여 물약 효과가 의도와 다르게 적용되는 문제가 있음.
- 해결 내역  
    - PR #35 #29 #30 이슈 해결에서 물약 사용 로직을 수정하여 올바른 체력 증가/회복이 되도록 처리하고 병합됨.

---

Issue #30 – 사용자 문자 입력 시 게임 오류
- 문제 증상  
    - scanf 사용 시 문자 입력 등이 들어오면 입력 예외 처리 미흡으로 게임이 오동작/오류를 일으키는 문제가 있음.
- 해결 내역  
    - PR #33 [Fix] #30 scanf 대체 함수 scani 추가에서 scanf를 대체하는 scani 함수를 추가하고,
PR #35 #29 #30 이슈 해결 및 이후 PR #68 [Fix] scani 수정, 변경 값들 롤백, Q3_4 스크립트 수정에서 scani 구현 및 입력 처리 로직을 보완하여 입력 안정성을 개선함.

---

Issue #31 – Run away 구현
- 문제 증상  
    - set() 등에서 도망가기(Run_away) 기능이 미구현 상태라 게임 로직상 필요한 행동을 할 수 없음.
- 해결 내역  
    - PR #45 [Feat] #31 Run Away 구현에서 Run_away() 기능을 구현하여 관련 호출부에 연결하고 병합됨.
      요청하신 이슈(#32, #34, #39, #48, #51, #56, #66)에 대해 정보를 수집했고, 먼저 도구로 받아온 원본 항목 전체를 그대로(절대 누락 없이) 보여드립니다. 그 다음 각 이슈별 요약(증상, 본문 요지, 상태/해결 정보)을 한국어로 정리했습니다.

---

- #32 Condition 칸 바로 못나감
  - 본문 요지: Condition 창에서 바로 빠져나가지 못하는 문제(화면을 두 번 눌러야 나가짐) 보고.
  - 증상: Condition 창(상태창?)에서 의도한 즉시 빠져나오지 못함.
  - 처리/해결: 이슈는 닫힘(closed). (닫은 사람: typemnm). 관련 수정은 해당 PR들 중 UI/입력 처리 개선 PR들에서 반영되었을 가능성 있음(직접 연결 PR 표시는 이 이슈의 본문/댓글에 없음).

---

- #34 Potion() 잘못된 번호 사용 시 맥스터 데미지 들어가요?
  - 본문 요지: 물약 사용 시 잘못된 번호 입력(예: 입력값 11 등) 상황에서 에러 메시지("You don't have that potion!!!")가 뜨지 않거나, 선택지로 잘못 빠져나가는 문제가 보고됨. (본문에 이미지 첨부)
  - 증상: 잘못된 입력 처리/검증이 부족해 예상치 못한 동작이 발생.
  - 처리/해결: 이슈는 닫힘. 관련 수정은 PR #40 "[Fix] #34 Potion() 잘못된 번호 입력 시 몬스터 데미지 방지" 등에서 해결된 것으로 보입니다.

---

- #39 [refactor] set_monster 적용  (이 항목은 PR 형태로 존재)
  - 본문/요지: set_monster 적용 관련 리팩터링/적용 작업. PR로 열려 병합됨(merged).
  - 증상/의도: 몬스터 설정 로직을 정리·적용하려는 리팩터링.
  - 처리/해결: PR #39이 병합됨(merged at 2025-11-12T06:25:34Z). 관련 이슈(예: 몬스터 관련 버그/질문)들이 해결되었음.

---

Issue #41 – set() 함수 내 비정상적 종료 출력
- 문제 증상  
    - set() 함수에서 게임오버 시 exit(1)을 사용하여 프로그램이 비정상 종료 메시지를 출력하는 등 종료 방식이 부자연스러움.
- 해결 내역  
    - PR #42 [Fix] #41 set() 내 exit(1) 수정에서 종료 처리 방식을 수정하여 보다 자연스럽게 게임 종료가 이뤄지도록 하고 병합됨.

---

Issue #44 – user.defence 검증 오류
- 문제 증상  
    - user.defence 검증이 Defence_Store가 아닌 다른 곳, 특히 Play_1() 등에서 제대로 이루어지지 않아 아이템/디펜스 값에 대한 보안 문제나 BOF(버퍼 오버플로우) 가능성이 제기됨.
- 해결 내역  
    - PR #55 [Fix] #44 Play_1()의 아이템 개수, 디펜스 검증 부분 상점으로 이동에서 검증 로직을 상점(Defence_Store) 쪽으로 이동하는 등 구조를 수정하여 문제를 해결하고 병합됨.

---


- #48 마법 버그
  - 본문 요지: 마법 관련 메서드 동작(입력/처리)에서 0/1 처리 관련 문제 보고. (본문: “종종 전투 시에 마법이 씹혀서... 0은 입력, 1에 대한 입력을 인식 못한다” 등)
  - 증상: 마법 입력/처리 로직의 경계/케이스 처리가 잘못되어 특정 입력이 무시되거나 오류 발생.
  - 처리/해결: 이슈는 닫힘(클로저: woohyun212). 서브 이슈가 1개 연결되어 있었음(연관된 추가 수정 이슈 존재).

---

- #51 몬스터 레벨 중복 지정
  - 본문 요지: set_monster 함수에서 레벨을 적절히 지정하지 않아 monster.lv = 1; 처럼 레벨이 항상 1로 설정되는 경우 등, 레벨 지정 로직 중복/오작동 의심.
  - 증상: 몬스터 레벨이 의도한 대로 설정되지 않음(중복 지정 혹은 고정값 지정).
  - 처리/해결: 이슈는 닫힘(클로저: woohyun212). 댓글/토론과 이미지가 있었고, 해결된 것으로 표시(최근 업데이트, closed).

---

- #56 캐릭터 선택 시 다른 정수 입력 시 세그먼트(이미지 첨부)
  - 본문 요지: 캐릭터 선택 화면에서 잘못된(다른) 정수 입력 시 화면/처리 문제가 발생한다는 증거(스크린샷) 첨부.
  - 증상: 잘못된 입력값에 대한 예외 처리가 필요함.
  - 처리/해결: 이슈는 닫힘(클로저: woohyun212). (이미지 첨부로 상황 확인 후 수정된 듯함)

---

- #66 goto문 제거
  - 본문 요지: goto문 제거 요청(코드 정리 목적).
  - 증상/요청: 코드에서 goto 문을 제거하여 리팩터링을 원함.
  - 처리/해결: 이슈는 닫힘(최근 닫음). 관련 리팩터링 PR(예: PR #67 "[refactor] goto문 제거")이 존재함(해당 PR이 닫히거나 병합되어 goto 제거가 반영된 상태).

---

## PR 기반 정리

PR #70
- 문제 증상 
   - 증상: 원래 코드에 #include <unistd.h>가 있었는데, 이 헤더는 유닉스/리눅스 전용으로 Windows에서 지원되지 않아 Windows 환경에서 빌드/호환성 문제가 발생할 수 있음.

- 해결 내역
    - Windows에서 지원되지 않는 유닉스 전용 헤더 관련 처리를 제거하거나 교체(플랫폼 대응으로 헤더 처리 수정). 메타데이터상 변경 파일 1개, 추가 1행, 삭제 1행으로 최소한의 수정이 이루어졌습니다.

---

PR #69 – [refactor] 수정사항 반영2
- 문제 증상  
    - 이전 리팩터링/수정 사항이 코드 전체에 완전히 반영되지 않아 정리/일관성 유지가 필요함.
- 해결 내역  
    - 추가 리팩터링을 통해 누락된 수정 사항들을 코드에 반영함. (세부 파일 정보는 PR 페이지 참고)

---

PR #67 – [refactor] goto문 제거
- 문제 증상  
    - 코드에 goto 문이 사용되어 가독성 및 유지 보수성 저하, 제어 흐름 파악이 어려운 문제가 있음.
- 해결 내역  
    - goto를 제거하고 구조화된 제어 흐름으로 변경하여 코드 품질을 개선함(병합 완료).

---

PR #68 – [Fix] scani 수정, 변경 값들 롤백, Q3_4 스크립트 수정
- 문제 증상  
    - scani 함수 및 일부 변경값, Q3/4 스크립트에서 의도와 다른 동작 또는 부작용이 발생함.
- 해결 내역  
    - scani 구현을 수정하고 문제를 일으키던 값들을 롤백하며, Q3_4 스크립트를 조정하여 시나리오와 입력 처리가 안정적으로 동작하도록 수정하고 병합함.

---

PR #65 – [fix] l_m값 기준 조정 및 Q3 대사 출력 수정
- 문제 증상  
    - l_m 값 기준이 잘못되어 레벨/상태에 따른 처리가 의도와 다르게 동작하거나, Q3 대사 출력이 시나리오와 맞지 않음.
- 해결 내역  
    - l_m 기준을 조정하고 Q3 대사 출력 로직을 수정하여 게임 진행과 대사가 자연스럽게 이어지도록 함.

---

PR #64 – [Fix] #27 #28 issue 해결; Battle() 함수 수정
- 문제 증상  
    - Issue #27, #28에서 제기된 것처럼 Battle() 함수에 출력 비효율/지연, 리소스 누수 문제가 존재함.
- 해결 내역  
    - 파일 입출력 및 출력 로직을 정리하고, 자원 관리 코드를 보완하여 Battle 함수의 안정성과 효율성을 개선하고 병합함.

---

PR #63 – Q4 페이즈 로직 수정
- 문제 증상  
    - Q4 보스/페이즈 진행 로직이 시나리오와 맞지 않거나 비정상 동작을 보임.
- 해결 내역  
    - Q4 페이즈 전개 순서와 조건 등을 수정하여 보스 페이즈가 의도대로 진행되도록 함(병합됨).

---

PR #61 – Set-Magic 순환 호출 제거
- 문제 증상  
    - Set 관련 로직과 Magic 관련 로직 사이에 순환 호출(서로를 계속 호출)이 발생해 무한 호출/재귀 또는 복잡한 흐름 문제가 생김.
- 해결 내역  
    - Set–Magic 사이의 순환 호출을 제거하여 호출 구조를 단방향으로 정리하고, 무한 호출 위험을 해소함.

---

PR #60 – issue#27 battle 함수 최적화
- 문제 증상  
    - Battle 함수가 성능/출력 측면에서 비효율적이며, Issue #27에서 제기된 문제를 내포함.
- 해결 내역  
    - 반복 출력 및 지연 로직 등을 최적화하여 Battle 함수에 대한 성능과 출력 품질을 개선하고 병합함.

---

PR #59 – [fix] 빠와쏘드 체력 음수 게임오버 처리
- 문제 증상  
    - 특정 캐릭터(빠와쏘드)의 체력이 음수로 내려가는 상황에서 게임오버 처리 로직이 적절히 동작하지 않거나 이상 동작을 유발함.
- 해결 내역  
    - 체력이 0 이하가 되는 경우를 안전하게 처리하는 로직을 추가/수정하여 게임오버가 정상적으로 발생하도록 함.

---

PR #58 – [refactor] 불필요 코드 제거
- 문제 증상  
    - 프로젝트 내에 더 이상 사용되지 않는 불필요한 코드가 남아 있어 가독성과 유지보수성이 떨어짐.
- 해결 내역  
    - 사용되지 않는 코드를 제거하여 코드 베이스를 정리하고, 리팩터링을 통해 구조를 단순화함.

---

PR #57 – [Fix] Opening 캐릭터 선택 로직 오류 해결
- 문제 증상  
    - 오프닝 단계에서 캐릭터 선택 로직이 잘못 구현되어 선택이 제대로 반영되지 않거나 의도와 다른 캐릭터가 선택되는 문제 발생.
- 해결 내역  
    - 캐릭터 선택 조건 및 분기문을 수정하여, 사용자의 선택이 정확히 반영되도록 로직을 수정하고 병합함.

---

PR #55 – [Fix] #44 Play_1()의 아이템 개수, 디펜스 검증 부분 상점으로 이동
- 문제 증상  
    - Play_1()에서 아이템 개수와 user.defence 값을 직접 검증하여 역할이 분산되고 검증 위치가 부적절한 구조를 갖고 있었음.
- 해결 내역  
    - 검증 로직을 상점(Defence_Store) 로직으로 이동해 책임을 명확히 하고, Issue #44에서 제기된 검증 문제를 해결함.

---

PR #54 – [Fix] 중복 코드 제거
- 문제 증상  
    - 동일/유사 기능을 하는 중복 코드가 여러 곳에 존재하여 수정 시 일관성 유지가 어렵고 유지보수가 힘듦.
- 해결 내역  
    - 중복 부분을 통합/정리하여 재사용 가능한 구조로 바꾸고, 코드 중복을 제거함.

---

PR #52 – 몬스터 설정 함수 통합 및 개선
- 문제 증상  
    - 몬스터 설정 관련 함수가 분산·중복되어 있어 몬스터 스탯/설정을 관리하기가 복잡함.
- 해결 내역  
    - 몬스터 설정 함수들을 통합하고 로직을 개선하여 몬스터 설정이 일관되게 관리되도록 함(병합됨).

---

PR #50 – 몬스터 설정 함수 통합 및 개선
- 문제 증상  
    - 위와 동일하게 몬스터 설정 로직의 분산 및 복잡도 문제가 존재.
- 해결 내역  
    - 몬스터 설정을 통합·개선하려는 추가 작업을 진행함(메타 정보상 병합 여부는 명시 X).

---

PR #49 – 윈도우 환경 콘솔 출력 코드 추가
- 문제 증상  
    - 윈도우 환경에서 콘솔 출력(색상, 형식 등)을 적절히 표현하기 어려운 환경 의존성 문제가 있음.
- 해결 내역  
    - 윈도우 전용 콘솔 출력 코드를 추가해 색상/형식을 제어하고, 플랫폼별 출력 경험을 개선함(병합).

---

PR #47 – [Fix] set() 내 l_m 감소 로직 수정
- 문제 증상  
    - set() 함수 내에서 l_m 감소 로직이 잘못되어 레벨/상태 값이 의도와 다르게 감소하는 버그가 발생함.
- 해결 내역  
    - 감소 조건 및 값을 수정하여 l_m이 정상적인 규칙에 따라 변경되도록 함.

---

PR #46 – [FEAT] textcolor
- 문제 증상  
    - 텍스트 색상 제어 기능이 없어 시각적인 피드백/가독성이 떨어짐.
- 해결 내역  
    - textcolor 기능을 추가해 상황에 따라 텍스트 색상을 변경할 수 있도록 하고, UI/가독성을 개선함.

---

PR #45 – [Feat] #31 Run Away 구현
- 문제 증상  
    - Issue #31에서 요구된 것처럼 도망가기 기능이 미구현 상태라 전투에서 선택지 부족.
- 해결 내역  
    - Run_away() 기능을 구현하고 전투 흐름에 연결해 플레이어가 전투를 이탈할 수 있도록 함.

---

PR #42 – [Fix] #41 set() 내 exit(1) 수정
- 문제 증상  
    - Issue #41에서 지적된 대로 set() 내 exit(1) 호출로 비정상 종료 메시지 및 불필요한 오류 종료가 발생함.
- 해결 내역  
    - 종료 로직을 수정해 정상적인 게임 종료 흐름으로 전환하고, 불필요한 오류 메시지를 제거함.

---

PR #40 – [Fix] #34 Potion() 잘못된 번호 입력 시 몬스터 데미지 방지
- 문제 증상  
    - Potion()에서 잘못된 번호를 입력해도 몬스터에게 데미지가 들어가는 비정상 동작이 발생함.
- 해결 내역  
    - 입력 번호 검증을 강화하여 잘못된 입력일 경우 몬스터에게 피해가 가지 않도록 로직을 수정함.

---

PR #39 – [refactor] set_monster 적용
- 문제 증상  
    - 몬스터 설정이 여러 곳에서 따로 이뤄져 설정 중복 및 일관성 문제가 있음.
- 해결 내역  
    - set_monster 함수를 도입/적용해 몬스터 설정을 한 곳에서 처리하도록 리팩터링함.

---

PR #38 – [Fix] issue#26 무한 재귀 해결
- 문제 증상  
    - Issue #26에서처럼 Play_1()의 무한 재귀로 게임이 멈추거나 스택 오버플로우가 발생.
- 해결 내역  
    - 재귀 호출 구조를 수정하여 무한 재귀를 제거하고, 정상적인 흐름으로 진행되도록 함(병합).

---

PR #37 – [feat] flush_stdin 함수 구현/적용
- 문제 증상  
    - 입력 버퍼에 남은 값 때문에 다음 입력이 꼬이는 문제가 발생할 수 있음.
- 해결 내역  
    - flush_stdin 함수를 구현해 입력 버퍼를 비우고, 필요한 곳에 적용하여 입력 안정성을 향상시킴.

---

PR #35 – #29 #30 이슈 해결
- 문제 증상  
    - Issue #29의 물약 능력 분리 오류, Issue #30의 문자 입력으로 인한 scanf 예외 문제 등 물약/입력 처리 관련 버그가 존재함.
- 해결 내역  
    - 물약 사용 로직과 입력 처리 로직을 수정해 두 이슈를 해결하고 병합함.

---

PR #33 – [Fix] #30 scanf 대체 함수 scani 추가
- 문제 증상  
    - scanf 사용 시 문자 입력 등으로 인해 입력 예외 및 게임 오동작이 발생함.
- 해결 내역  
    - 보다 안전한 입력 처리를 위해 scani 함수를 새로 구현·추가하여 scanf를 대체함. (PR은 close 처리, merged 필드는 비어 있음)

---

PR #24 – 몬스터 수치 조정
- 문제 증상  
    - 몬스터의 체력/공격력 등 수치 밸런스가 적절하지 않아 게임 난이도가 불균형함.
- 해결 내역  
    - 몬스터 스탯 수치를 조정해 난이도 밸런스를 맞추고 게임 플레이 경험을 개선함(병합).

---

PR #23 – 몬스터 난이도 조정
- 문제 증상  
    - 몬스터의 난이도가 전체 진행 흐름에 비해 너무 쉽거나/어려운 구간이 존재함.
- 해결 내역  
    - 몬스터 난이도를 조정해 게임 전체 난이도 곡선을 다듬음(메타상 merged 필드는 비어 있음).

---

PR #22 – [Feat] Q2
- 문제 증상  
    - Q2 단계(퀘스트/시나리오)가 미구현 상태라 스토리 진행이 끊김.
- 해결 내역  
    - Q2 관련 기능과 시나리오를 구현해 게임 초반/중반 스토리가 이어지도록 추가하고 병합함.

---

PR #19 – Q4 시나리오에 맞춰 수정 및 보스 페이즈 추가
- 문제 증상  
    - Q4 시나리오 및 보스 페이즈가 기획된 내용과 코드가 맞지 않거나 부족한 상태였음.
- 해결 내역  
    - Q4 시나리오에 맞게 로직을 수정하고, 보스 페이즈를 추가 구현하여 엔딩 구간의 진행을 완성함(병합).

---